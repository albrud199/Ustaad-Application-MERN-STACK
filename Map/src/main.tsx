import React from "react";
import ReactDOM from "react-dom/client";
import UstaadMapDashboard from "../ustaad_map_dashboard_nextjs";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <UstaadMapDashboard />
  </React.StrictMode>,
);
